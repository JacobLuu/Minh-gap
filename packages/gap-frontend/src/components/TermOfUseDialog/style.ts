import { styled } from '@mui/material/styles';
import { Square } from '@mui/icons-material';
import { LIST_ICON_FONT_SIZE } from '../../themes/Fonts';

export const ListIcon = styled(Square)`
  font-size: ${LIST_ICON_FONT_SIZE}px;
`;
